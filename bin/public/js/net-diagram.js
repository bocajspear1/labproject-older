function network_diagram()
	{
		this.init = init;
		function init(canvas_holder)
			{
				jdiagram.init(canvas_holder);
			}
		
		this.add_device = add_device;
		function add_device(device_info)
			{
				
			}
	}
